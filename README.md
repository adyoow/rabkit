# rabkit

## Install

```shell
npm install --save rabkit
```

## Usage

```js
// using ES6 modules
import { isArray } from 'rabkit'

// using CommonJS modules
const { isArray } = require('rabkit')

isArray([]) // true
```
